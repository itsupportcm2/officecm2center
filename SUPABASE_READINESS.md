# เตรียมระบบก่อนเชื่อม Supabase

เอกสารนี้เป็นขั้นตอนสำหรับวันที่พร้อมย้ายจากโหมดทดสอบในเครื่องไปใช้ฐานข้อมูลออนไลน์ ปัจจุบันยังไม่ต้องใส่ URL หรือ Key และยังไม่ต้องรัน SQL บนระบบจริง

## สิ่งที่เตรียมไว้แล้ว

- ยอดสินค้ารวมถูกคำนวณจากทุกตำแหน่ง โดยไม่สร้างแถวสินค้าซ้ำ
- สินค้ามีตำแหน่งหลัก และการเพิ่มสินค้า/ยอดเริ่มต้นทำใน transaction เดียว
- การรับเข้าและเบิกออกเก็บผู้จำหน่ายและผู้อนุมัติเป็นฟิลด์แยกสำหรับรายงาน
- จำกัดชื่อแผนกตามรายการที่กำหนดไว้ในหน้าจอ
- ปิดช่องทางที่ผู้ใช้ทั่วไปสามารถแก้ role ของตัวเอง
- ผู้ใช้ใหม่จะถูกสร้าง profile เป็น `viewer` โดยอัตโนมัติ
- หน้า Admin จัดการบัญชี Authentication จริงผ่าน Edge Function และระงับผู้ใช้ได้
- คุมสถานะใบขอซื้อ: staff สร้าง/แก้ draft และส่งอนุมัติได้ แต่สั่งซื้อหรือยกเลิกต้องเป็น admin
- การต่ออายุพร้อมประวัติทำแบบ atomic ผ่าน RPC
- ฟังก์ชันภายในถูกถอนสิทธิ์เรียกตรง และเปิดเฉพาะ RPC ที่จำเป็น

## ลำดับติดตั้งฐานข้อมูลใหม่

รันไฟล์ต่อไปนี้ใน SQL Editor ตามลำดับ:

1. `supabase/migrations/202609220001_initial_schema.sql`
2. `supabase/migrations/202609230001_hardening.sql`
3. `supabase/migrations/202609240001_online_readiness.sql`
4. `supabase/migrations/202609240002_online_contexts.sql`
5. `supabase/migrations/202609240003_fix_audit_trigger.sql`
6. `supabase/migrations/202609250001_user_management.sql`
7. Deploy `supabase/functions/manage-users`
8. `supabase/seed.sql` เฉพาะเมื่อต้องการข้อมูลตัวอย่าง

จากนั้นสร้างผู้ใช้คนแรกใน Supabase Authentication แล้วเปลี่ยน profile ของบัญชีนั้นเป็น admin ด้วย SQL Editor ที่เชื่อถือได้:

```sql
update public.profiles
set role = 'admin', full_name = 'ชื่อผู้ดูแลระบบ'
where id = 'AUTH_USER_UUID';
```

ห้ามนำ service-role key ไปใส่ในเว็บหรือไฟล์ `.env.local` ฝั่ง frontend ใช้เฉพาะ anon key เท่านั้น

## ตารางสิทธิ์

| งาน | admin | staff | viewer |
|---|---:|---:|---:|
| ดูสินค้า/ยอด/ประวัติ | ✓ | ✓ | ✓ |
| รับเข้า/เบิกออก | ✓ | ✓ | — |
| ปรับยอด/โอนตำแหน่ง | ✓ | ✓ | — |
| เพิ่ม/แก้/ลบสินค้า | ✓ | — | — |
| หมวดหมู่/ตำแหน่ง/ผู้ใช้/ตั้งค่า | ✓ | — | — |
| สร้าง draft และส่งใบขอซื้อ | ✓ | ✓ | — |
| ยืนยันสั่งซื้อ/ยกเลิกใบขอซื้อ | ✓ | — | — |
| ดู Audit Log | ✓ | — | — |

## Checklist ก่อนเปิดใช้จริง

- สำรองฐานข้อมูลก่อนรัน migration ทุกครั้ง
- ทดลองด้วยบัญชี admin, staff และ viewer แยกกัน
- ตรวจรับเข้า เบิกออก ยอดติดลบ การโอน และการปรับยอดพร้อมกันสองหน้าต่าง
- ตรวจว่ารายงานแสดงผู้รับสินค้า แผนก และผู้อนุมัติครบ
- ตรวจ popup ต่ออายุและการบันทึกประวัติต่ออายุ
- ตั้งค่า Site URL และ Redirect URL ให้ตรงโดเมนจริง
- เปิด MFA ให้บัญชี admin และกำหนดผู้ดูแลอย่างน้อยสองคน
- วางแผน backup/restore และทดสอบกู้คืนก่อนใช้งานจริง

## งานที่ยังไม่ควรถือว่าออนไลน์สมบูรณ์

หน้าสต็อก รายการต่ออายุ ใบขอซื้อ ตั้งค่า Audit Log และการจัดการผู้ใช้รองรับ Supabase แล้ว แต่ยังต้องรัน migration, deploy Edge Function และทดสอบ end-to-end ด้วยบัญชี admin, staff และ viewer ในโปรเจกต์ทดสอบก่อนเปิดให้พนักงานใช้
